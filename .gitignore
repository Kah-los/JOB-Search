# Personal / sensitive — keep OUT of the public repo
# (NOTE: git does NOT support inline comments after a pattern, so keep them on their own line)
# tailored CVs + cover letters with full contact details
applications/
# name, email, phone, and visa-sponsorship status
pipeline/profile.json

# Build/scratch artifacts
data/*.log
**/__pycache__/
*.pyc
.DS_Store

# Telegram bot credentials — never commit
pipeline/telegram_config.json

# Resume / personal documents — keep local, never publish
*.pdf
*Resume*.pdf
*-CV-*.md
